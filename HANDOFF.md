# HANDOFF
Started process of porting legacy Java/C++ engines to TypeScript by adding them as submodules.
Submodules added: `bobsgameonlinejava` and `okgame`.
Attempted skeleton architecture but reverted due to massive type checker errors.
Project builds successfully now.
