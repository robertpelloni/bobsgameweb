# MEMORY

- The user wants EXTREMELY detailed documentation.
- The user uses exuberant adjectives ("Absolutely outstanding", "Insanely great").
- Always use a global version number synced across `VERSION.md`, `CHANGELOG.md`, and `package.json`.
- Do not stop until all features are implemented. Keep iterating.
- Use subagents when possible.
- Continuously update docs, HANDOFF.md, and commit/push regularly.
